# ABB.IO — передача разработки

Дата актуализации: 12 сентября 2026.

## Единственная рабочая папка

Продолжайте работу **только** здесь:

`C:\AI Work\AI Work\Codex\Маркетинговое агенство\Сайт`

Это уже существующий Next.js-проект и его локальная рабочая копия. Не создавайте новый проект, не инициализируйте новый Git-репозиторий, не клонируйте сайт в другую папку и не переносите компоненты в новый шаблон.

Перед началом прочитайте:

1. `C:\AI Work\AI Work\Codex\Маркетинговое агенство\AGENTS.md`;
2. `C:\Users\reddingtonlovitz\Documents\Obsidian Mind\work\active\ABB — Карта проекта и база знаний.md`;
3. этот файл;
4. `PROJECT_CONTEXT.md`;
5. `docs/frontend/DESIGN.md`;
6. `docs/frontend/HOMEPAGE-REVIEW.md`;
7. затем — только относящийся к задаче компонент и CSS Module.

## Репозиторий и запуск

- Рабочая ветка: `publish-abbio`, она отслеживает `abbio/main`.
- Целевой remote: `abbio` → `https://github.com/allbobrov-cloud/abbio.git`.
- `origin` и локальная ветка `main` относятся к историческому репозиторию `MarketingAgency`; не переключайтесь на них и не пушьте туда без прямой просьбы владельца.
- Публикация ABB.IO уже была сделана в `abbio/main` коммитом `7b2836b` (`Publish ABB.IO website`). Последующие локальные изменения сами по себе не опубликованы.

Перед Git-операциями обязательно выполните `git status --short`, сохраните чужие изменения и не используйте разрушительные `git reset --hard` или `git checkout --`.

Локальный запуск:

```powershell
cd "C:\AI Work\AI Work\Codex\Маркетинговое агенство\Сайт"
npm run dev
```

Обычно сайт открывается на `http://127.0.0.1:3000`. Если сервер уже запущен, не поднимайте второй экземпляр без необходимости.

## Что уже есть на главной

12 сентября 2026 владелец утвердил текущую главную по содержанию, визуалу и композиции. Новые блоки ей не нужны; при разработке внутренних страниц главную не расширять и не использовать как шаблон для прямого копирования.

Порядок в `src/app/page.tsx`: hero → ситуации клиента → услуги → кейсы → демо-отчёт → отраслевой блок → путь клиента → процесс → форматы работы → команда. Контактная форма находится в footer, а общая модальная форма открывается из CTA.

## Следующая очередь разработки

Утверждённый порядок внутренних страниц: **Сайты → Маркетинг → Дизайн**. Канонический roadmap и автономные задания находятся в Obsidian:

- `C:\Users\reddingtonlovitz\Documents\Obsidian Mind\work\active\ABB — План развития сайта.md`;
- `C:\Users\reddingtonlovitz\Documents\Obsidian Mind\work\active\ABB — Задания Codex для внутренних страниц.md`.

Каждый следующий этап начинать после просмотра владельцем предыдущей страницы. Сначала новый шаблон подключается только к `/services/websites`; затем на общей основе, но с самостоятельными композициями, развиваются `/services/marketing` и `/services/design`.

Актуализация от 12 сентября 2026: business-first версия `/services/websites` локально собрана и прошла строгую перепроверку, но ещё ожидает просмотра владельца — прежняя отметка об утверждении не восстановлена. Страница строится от коммерческой задачи: полноэкранный hero говорит о заявках и передаче обращения в CRM; далее отдельные блоки раскрывают выбор формата, путь посетителя, технологию, CRM, измеримость, дизайн, состав передачи, кейс и CTA. Подтверждённые технологии — React / Next.js, WordPress и 1С-Битрикс. В QA добавлена контекстная ссылка на `/services/marketing`, metadata приведена к H1; проверены 1440/1280/1024/390/360/320, modal, меню, focus, reduced motion, изображения и связанные маршруты. `npm run lint` и `npm run build` проходят. Публикация не выполнялась. Подробности зафиксированы в [[ABB — План развития сайта]].

Актуализация от 12 сентября 2026: локально завершена переработка `/services/marketing`; она также ожидает просмотра владельца. Страница объясняет маркетинг через источники обращений, путь спроса до заявки, Битрикс24, категории показателей, заметный демо-отчёт с явно условными данными, SEO как измеряемый канал, цикл корректировок и конкретный состав результата для бизнеса. В строгом QA исправлен единственный подтверждённый дефект: transition новых SEO-ссылок добавлен в `prefers-reduced-motion`. Проверены 1440×900, 1280×720, 1024×768, 390×844, 360×800 и 320×700: горизонтального overflow нет, изображения загружаются, CTA-dialog работает, Escape возвращает focus, внутренние ссылки и внешняя ссылка Google Sheets корректны; console errors/warnings = 0. `npm run lint` и `npm run build` проходят. Публикация не выполнялась.

Ключевые файлы:

- `src/components/home/HomeExperience.tsx` и `HomeExperience.module.css` — hero, ситуации, путь клиента, форматы;
- `src/components/home/HomePortfolio.tsx` и `HomePortfolio.module.css` — услуги, отчёт, отраслевой блок;
- `src/components/CasesSection.tsx` и `CasesSection.module.css` — кейсы;
- `src/components/AgencyHeader.tsx`, `AgencySections.tsx`, `Agency.module.css` — навигация, процесс, команда, footer;
- `src/components/ContactDialog.tsx`, `FooterContactForm.tsx`, `src/lib/contactForm.ts` — формы и маска телефона;
- `src/lib/content.ts` — редактируемые тексты и данные.

## Неподвижные рамки

- Визуальный язык: тёмный premium B2B/editorial, графитово-синий фон, фиолетовый акцент, строгая сетка, крупная типографика, спокойные интеракции.
- Один блок отвечает на один новый вопрос посетителя. Не возвращайте повторы о «системе», «росте» и «аналитике» под разными заголовками.
- Не придумывайте реальные кейсы, клиентские цифры, гарантии, контакты, состав команды или отзывы. Метрики возможны только с источником и периодом.
- Иллюстрации AI и интерфейсные схемы — декоративные, не доказательства результатов. Демо Google Sheets — пример структуры отчёта, не клиентские показатели.
- У форм сейчас нет backend-доставки: валидная отправка должна честно сообщать, что данные не отправлены и не сохранены, пока владелец не подключит сценарий.
- Не затрагивайте главную, другие внутренние страницы, Git-историю или внешние сервисы за пределами конкретного задания владельца.

## Текущий уровень и проверка

Последний мобильный quality pass проверял главную на 390×844, 360×800 и 320×700: нет горизонтального overflow и ошибок консоли; меню, направления hero, сценарии и модальная форма работают; touch-цели не меньше 24px; остаётся одно активное изображение hero; snap-ленты имеют доступные подписи. `npm run lint` и production `npm run build` проходили для актуального кандидата.

После правок фронтенда запускайте:

```powershell
npm run lint
npm run build
```

При изменении интерфейса быстро проверьте desktop и mobile, меню, CTA, переключатели, фокус с клавиатуры, отсутствие переполнения и console errors. Не заявляйте, что это полный аудит WCAG, SEO или производительности.

## Готовый запрос для нового чата

```text
Продолжаем разработку существующего сайта агентства ABB.IO.

Работай только в папке:
C:\AI Work\AI Work\Codex\Маркетинговое агенство\Сайт

Это существующий Next.js-проект. Не создавай новый проект, не клонируй репозиторий, не инициализируй Git и не переноси сайт в другую папку.

Сначала прочитай:
1) C:\AI Work\AI Work\Codex\Маркетинговое агенство\AGENTS.md
2) C:\Users\reddingtonlovitz\Documents\Obsidian Mind\work\active\ABB — Карта проекта и база знаний.md
3) C:\Users\reddingtonlovitz\Documents\Obsidian Mind\work\active\ABB — План развития сайта.md
4) C:\Users\reddingtonlovitz\Documents\Obsidian Mind\work\active\ABB — Задания Codex для внутренних страниц.md
5) C:\AI Work\AI Work\Codex\Маркетинговое агенство\Сайт\docs\SESSION_HANDOFF.md
6) C:\AI Work\AI Work\Codex\Маркетинговое агенство\Сайт\PROJECT_CONTEXT.md
7) C:\AI Work\AI Work\Codex\Маркетинговое агенство\Сайт\docs\frontend\DESIGN.md
8) C:\AI Work\AI Work\Codex\Маркетинговое агенство\Сайт\docs\frontend\HOMEPAGE-REVIEW.md
9) Затем только исходники, относящиеся к текущей внутренней странице.

Рабочая ветка — publish-abbio, она отслеживает remote abbio/main (https://github.com/allbobrov-cloud/abbio.git). Старые origin и main не трогай. Перед любыми Git-командами проверь git status --short и сохрани имеющиеся изменения.

Главная ABB.IO утверждена и не должна расширяться или перестраиваться. Следующая работа — внутренние страницы в порядке «Сайты → Маркетинг → Дизайн» по отдельным заданиям из Obsidian. Сохраняй тёмный premium B2B/editorial стиль, фиолетовый акцент, строгую сетку и лаконичные конкретные тексты. Не придумывай реальные результаты, метрики, контакты, команду или гарантии. Не публикуй изменения без моего прямого запроса.

После frontend-правок запусти npm run lint и npm run build. Если менялась вёрстка, быстро проверь desktop/mobile, отсутствие horizontal overflow, фокус, меню, CTA и console errors. В финале перечисли изменённые файлы, проверки и оставшиеся открытые вопросы.
```

## Демо-проект ARC 01 в «Развитии идеи» — 13 сентября 2026

На `/services/design` заменено только наполнение блока `flow-title` по утверждённому владельцем визуальному ориентиру. Пять этапов теперь показывают один вымышленный проект ARC 01: бриф, artboard, UI kit, desktop/mobile-страница товара, сайт/презентация/коммуникационный носитель. Актуальный статус и результаты проверки находятся в основной карте ABB; финальное исполнение ещё ожидает просмотра владельца.

Код изолирован в `src/components/services/DesignFlow.tsx` и `DesignFlow.module.css`; `DesignServicePage.tsx` подключает компонент и содержит уточнённую подпись демо. В CSS страницы добавлены только правила ширины/ритма целевой секции. Реальные тексты, цена, кнопки, сетки и элементы интерфейса не запекаются в картинки. Продуктовые рендеры: `public/services/arc-01-hero-v1.webp` и `arc-01-angle-v1.webp`. Точные промпты, размеры, ограничения и описание QA — [ARC01-DEMO.md](frontend/ARC01-DEMO.md).

Демо-кнопки меняют только локальный статус, без заказа/оплаты. Маркировку «ДЕМО-КОНЦЕПТ» и пояснение о вымышленных продукте, характеристиках и цене сохранять. По последней правке владельца пять карточек на широком desktop выровнены по общей верхней и нижней линии без фиксированной высоты; на mobile все пять этапов и изображения видны с естественной высотой контента. Главная, hero и другие секции этой правкой не затронуты. Публикация не выполнялась.

## Витрина передачи ARC 01 — 13 сентября 2026

Секция `05 / Передача результата` на `/services/design` больше не использует одинаковые абстрактные карточки. Новый `src/components/services/DesignDeliverables.tsx` показывает шесть материалов одного условного проекта ARC 01 в 12-колоночной bento-сетке: concept board, набор artboards, identity-систему с условием «если это входит в задачу», desktop/mobile макеты, презентацию и состав передачи. Важные тексты, статусы, swatches и интерфейсные детали собраны HTML/CSS. По прямым визуальным правкам владельца здесь больше не используются белые предметные рендеры из блока «Развитие идеи»: добавлены отдельные изображения `arc-01-direction-v1.webp` (moodboard визуального направления: палитра, фактуры, свет и композиция), `arc-01-devices-v1.webp` (согласованные desktop- и mobile-макеты на мониторе и телефоне), `arc-01-pedestal-v1.webp` (предметная сцена с синим акрилом, лаймовым диском и оранжевой бумажной формой), `arc-01-night-v1.webp` (ночная рекламная композиция с направленным тёплым светом) и `arc-01-figma-layout-v1.webp` (Figma-подобное рабочее пространство с desktop/mobile-артбордами, слоями и свойствами). Карточка «Подготовленные исходники» показывает последний визуал вместо схемы файлов. На широком desktop два ряда имеют по 282 px, на tablet/mobile сетка перестраивается без фиксированной высоты. Сохранять маркировку `ARC 01 / ДЕМО-КОНЦЕПТ`.

`npm run lint` проходит. `npm run build` успешно компилирует новую витрину, но останавливается на уже существующей чужой ошибке `Cannot find name 'closeDialog'` в `src/components/ContactDialog.tsx:55`; этот файл не менялся в рамках витрины. Browser-preview повторно проверен после замены кампейн-визуалов: на 1440 px и 390 px изображения загружаются, root horizontal overflow отсутствует, console errors/warnings = 0. На 1440 шесть карт расположены как 4/5/3 и 5/4/3 колонки.

## Убран разделитель перед контактом — 13 сентября 2026

По прямой визуальной правке владельца из `.footer` в `src/components/Agency.module.css` удалена только верхняя граница. Это убирает длинную полосу между предшествующим блоком и контактной формой; её отступ, содержание и внутренний разделитель формы сохранены. `npm run lint` проходит. Desktop-preview `1440×900` подтверждает `border-top: 0px`, отсутствие horizontal overflow и console errors. Production build компилируется, затем останавливается на прежней не затронутой ошибке `Cannot find name 'closeDialog'` в `src/components/ContactDialog.tsx:55`.

## Витрина NORD FORM — 13 сентября 2026

По новой правке владельца только бенто-витрина `05 / Передача результата` на `/services/design` переведена с ARC 01 на вымышленный магазин женской одежды NORD FORM. Заголовки и описания шести карточек сохранены. Внутренние экраны, статусы, палитра и подписи теперь относятся к спокойному fashion-магазину: пальто, трикотаж, сумка, каталог, карточка товара и корзина. Созданы через встроенный imagegen и подключены новые финальные WebP: `public/services/nord-form-direction-v2.webp` (16:9 moodboard из пальто, фактур и палитры), `nord-form-devices-v2.webp` (desktop- и mobile-магазин на мониторе и телефоне), `nord-form-lookbook-v2.webp` (editorial-кадр с пальто), `nord-form-still-life-v2.webp` (трикотаж, сумка и балетки) и `nord-form-figma-layout-v2.webp` (Figma-подобное рабочее пространство с макетами магазина). Палитра — молочный, песочный, тауп, графит и приглушённый бордовый; насыщенные синий, оранжевый и лаймовый акценты из этой витрины не используются.

Изменены `DesignDeliverables.tsx`, `DesignDeliverables.module.css` и подпись демо в `DesignServicePage.tsx`. Desktop `1440×900` подтвердил загрузку всех новых изображений, отсутствие root horizontal overflow и console errors/warnings. В mobile viewport `390×844` root overflow также отсутствует; lazy-loading отложил загрузку одного невидимого изображения до прокрутки секции. `npm run lint` проходит. Production build компилируется, затем останавливается на прежней не затронутой ошибке `Cannot find name 'closeDialog'` в `src/components/ContactDialog.tsx:55`.

## Бесшовный hero «Дизайн» — 13 сентября 2026

По замечанию владельца в hero `/services/design` убран заметный вертикальный стык между фоном и монитором. В `DesignServicePage.module.css` фон hero сделан единым `#070912`; дополнительный псевдоэлемент, рисовавший отдельную тёмную поверхность слева от монитора, отключён. Само изображение монитора теперь постепенно растворяется через CSS mask, поэтому фон остаётся одним полем и не образует вертикальных полос. На `1920×1080` проверены отсутствие horizontal overflow, `::before { display: none }`, маска изображения и console errors/warnings. `npm run lint` проходит. Production build компилируется, затем останавливается на прежней не затронутой ошибке `Cannot find name 'closeDialog'` в `src/components/ContactDialog.tsx:55`.

## Уточнение карточек 03 и 06 в витрине NORD FORM — 13 сентября 2026

По замечанию владельца карточка «Элементы фирменного стиля» дополнена двумя мини-примерами применения системы: `Lookbook` и `Каталог`. Карточка «Подготовленные исходники» больше не остаётся пустым белым полем при прокрутке: preview рабочего пространства Figma загружается с `loading="eager"`, на время загрузки имеет тёмную информативную подложку и подпись, а под ним показан состав демо-файла: каталог, товар, корзина и стили. Содержимое остаётся условным NORD FORM, без заявлений о реальном проекте.

Изменены только `src/components/services/DesignDeliverables.tsx` и `src/components/services/DesignDeliverables.module.css`. Browser-проверка подтвердила загрузку Figma-preview (`320×142`) и отсутствие root horizontal overflow. `npm run lint` проходит. Production build компилируется, затем останавливается на прежней не затронутой ошибке `Cannot find name 'closeDialog'` в `src/components/ContactDialog.tsx:55`.

## Карточка «Подготовленные исходники» — 13 сентября 2026

По следующему замечанию владельца прежний image-preview, который на некоторых устройствах оставался тёмным пустым полем, полностью заменён на статический HTML/CSS-превью передаваемого Figma-файла. Оно показывает имя файла `NORD FORM / e-commerce`, список страниц «Каталог / Товар / Корзина», «Стили и компоненты» и три соответствующих artboard’а. Визуал не зависит от подгрузки изображения. Изменены только `DesignDeliverables.tsx` и `DesignDeliverables.module.css`; `npx eslint src/components/services/DesignDeliverables.tsx` проходит. Browser-проверка подтверждает заполненный canvas `393×138` и отсутствие root horizontal overflow.

## Универсальный блок кейсов — 13 сентября 2026

Одиночный case-блок /services/design заменён CasePortfolio: три существующих кейса из src/lib/content.ts (bogov, oss, volhonka), крупный featured кейс, переходы на каждый проект и ссылка /cases. Новый компонент и CSS Module предназначены для повторного подключения к страницам услуг.
pm run lint проходит; browser подтвердил 3 карточки, 4 ссылки, загрузку изображений и отсутствие overflow. Build по-прежнему останавливается на не связанной ошибке closeDialog в ContactDialog.tsx:55.

## Единая сетка заголовков — 13 сентября 2026

«04 / Развитие идеи» теперь использует ту же desktop-композицию заголовка, что и «05 / Передача результата»: индекс слева, заголовок по центру, пояснение справа; на tablet/mobile сохраняется последовательное чтение.
pm run lint проходит; root horizontal overflow отсутствует.

## Mobile audit — 13 сентября 2026

Проверены /, /services, /services/design, /services/websites, /services/marketing и /cases на 390 px. На ключевых маршрутах root horizontal overflow отсутствует, основные интерактивные цели не меньше 24 px. Исправлена одна найдена цель: breadcrumb «Главная» на /cases получил mobile min-height 28 px в Agency.module.css. Вне viewport lazy-изображения корректно остаются отложенными; это не ошибка загрузки.
pm run lint проходит.

## Повторная коррекция mobile `/services/design` — 13 сентября 2026

После отрицательной оценки владельца мобильная версия страницы пересобрана визуально, а не только проверена на overflow. В шапке при ширине до 760 px остаются логотип и 44 px menu-кнопка; CTA «Обсудить задачу» находится внутри раскрытого меню. Hero больше не использует `100svh` и поздние desktop-координаты изображения: заголовок уменьшен и удерживается в трёх строках, действия идут вертикально, квадратный monitor-visual показывается сразу под ними без огромной пустоты и обрезания. Контейнеры, заголовки и вертикальный ритм секций уплотнены; два иллюстрированных сценария снова показывают изображения на mobile, а их Next Image `sizes` соответствует реальной ширине. Карточки NORD FORM идут в смысловом порядке `01–06`.

Для локального preview на `127.0.0.1` в `next.config.ts` добавлен `allowedDevOrigins`, иначе Next.js 16 блокировал HMR и клиентская гидратация меню не происходила. В `ContactDialog.tsx` добавлен отсутствовавший `closeDialog`, поэтому прежний TypeScript-блокер устранён. Playwright-аудит на 390×844 и 360×800 проверил viewport-fit без horizontal overflow, 44 px menu/CTA-цели, открытие меню и Escape, последовательность карточек и загрузку всех изображений после прокрутки. Отдельными viewport-снимками просмотрены hero, preview, ситуации, работа, CTA, развитие идеи, результаты, кейсы и форма. `npm run lint` и production `npm run build` проходят. Финальный снимок первого экрана: `artifacts/design-mobile-390.png`. Публикация и push не выполнялись.

## Mobile hero с визуалом под заголовком — 13 сентября 2026

По уточнению владельца mobile hero `/services/design` получил последовательность: отдельный H1, затем основной monitor-visual, затем CTA. Подзаголовок «Формируем визуальный язык…» наложен на нижнюю часть изображения и защищён плотным тёмным градиентом и text-shadow. Верхняя граница изображения также растворена в фоне вертикальным градиентом, поэтому прямой горизонтальный стык не виден. Ссылка «Посмотреть ситуации» скрыта только на mobile, а «Обсудить задачу» размещена сразу после изображения. Desktop-композиция сохранена.

Контрольный viewport 390×844 подтверждает целостную композицию без horizontal overflow. `npm run lint` и production `npm run build` проходят. `git diff --check` не выявил whitespace errors. Финальный снимок: `artifacts/design-mobile-hero-final.png`. Публикация и push не выполнялись.

## Компактная mobile-секция «03 / Работа ABB.IO» — 13 сентября 2026

По замечанию владельца длинная вертикальная последовательность текстов в mobile-версии `/services/design` заменена горизонтальной scroll-snap лентой из четырёх компактных карточек. Каждая карточка получила номер, короткий заголовок задачи, три структурированных уровня «Визуальный принцип / Применение / На выходе» и отдельную акцентную плашку результата. Подпись «4 задачи · листайте» и видимый край следующей карточки объясняют жест без дополнительной кнопки. Desktop-таблица сохранена без изменений.

Визуально проверены viewport 390×844, 360×800 и desktop 1440×900. Секция больше не образует длинную текстовую «портянку», строки не обрезаются. `npm run lint`, production `npm run build` и `git diff --check` проходят. Снимок: `artifacts/design-mobile-work-cards.png`. Публикация и push не выполнялись.

## Центрированная CTA в mobile-карточке контакта — 13 сентября 2026

По замечанию владельца кнопка «Обсудить задачу» в mobile-карточке «Готовы обсудить дизайн-задачу?» центрирована по горизонтали и по вертикали внутри свободной нижней зоны изображения. Контейнер кнопки растянут на всю доступную ширину, поэтому на него больше не влияет desktop-прижатие вправо. Метка, заголовок и описание получили двухслойную тёмную text-shadow для читаемости на фоне рабочего пространства. Desktop-композиция не менялась.

Проверено на 390×844; финальный снимок: `artifacts/design-mobile-contact-centered.png`. `npm run lint`, production `npm run build` и `git diff --check` проходят. Публикация и push не выполнялись.

## Горизонтальный mobile-scroll блока «04 / Развитие идеи» — 13 сентября 2026

По замечанию владельца пять подробных этапов DesignFlow на mobile больше не складываются в длинную вертикальную колонку. Список переведён в горизонтальную scroll-snap ленту с перелистыванием вправо и влево. Первый этап занимает основную ширину, справа виден край следующей карточки, а в панели добавлена подсказка «5 этапов · листайте ↔». Содержимое и иллюстрации всех этапов сохранены. Desktop-сетка из пяти карточек не менялась.

Визуально проверены 390×844, 360×800 и desktop 1440×900. `npm run lint` и production `npm run build` проходят. Снимок: `artifacts/design-mobile-flow-carousel.png`. Публикация и push не выполнялись.

## Стык кейсов и контактного footer — 13 сентября 2026

По замечанию владельца устранён большой пустой зазор между последней карточкой CasePortfolio и блоком «Есть задача? Давайте обсудим». Причиной было сложение нижнего padding портфолио, внешнего margin футера и верхнего padding футера. Внешний margin убран; mobile-низ CasePortfolio сокращён до 28 px, mobile-верх footer — до 42 px. На границе footer добавлен единый тонкий разделитель `rgba(213,201,245,.14)`, продолжающий систему линий страницы.

Проверены mobile 390×844 и desktop 1440×900; разделитель виден, форма и карточки не обрезаются. `npm run lint`, production `npm run build` и `git diff --check` проходят. Снимок: `artifacts/design-mobile-footer-divider.png`. Публикация и push не выполнялись.

## Единый уровень содержимого карточек DesignFlow — 13 сентября 2026

По замечанию владельца в горизонтальной mobile-ленте «04 / Развитие идеи» выровнено начало внутренних макетов: заголовочная зона всех пяти карточек получила единую высоту 126 px с запасом под трёхстрочное название. Количество строк в заголовке больше не сдвигает белый рабочий экран вниз; карточки визуально начинаются на одной горизонтальной линии. Desktop не менялся.

Проверены 390×844 и 360×800; длинные заголовки помещаются, соседние поверхности выровнены. `npm run lint`, production `npm run build` и `git diff --check` проходят. Снимок: `artifacts/design-mobile-flow-aligned.png`. Публикация и push не выполнялись.

## Одинаковая полная высота карточек DesignFlow — 13 сентября 2026

После уточнения владельца выровнены не только заголовочные зоны, но и полная высота внутренних светлых поверхностей в горизонтальной mobile-ленте «04 / Развитие идеи». `.surface` теперь растягивается внутри одинаковых по высоте внешних карточек; короткие макеты доходят до общей нижней границы и не оставляют отдельное тёмное поле. Desktop не менялся.

Проверено в мобильной композиции 390 px, включая общий нижний край ленты и переход к секции «05 / Передача результата». `npm run lint`, production `npm run build` и `git diff --check` проходят. Снимок: `artifacts/design-mobile-flow-equal-height.png`. Публикация и push не выполнялись.

## Компактная высота карточек DesignFlow без пустого заполнения — 13 сентября 2026

Владелец отклонил растягивание коротких светлых макетов до высоты самой длинной карточки. Flex-растяжение `.surface` на mobile удалено: каждый внутренний макет снова заканчивается сразу после своего содержимого. Чтобы общая высота горизонтальной ленты оставалась ровной и компактной, пятая карточка «Применение» пересобрана в плотную двухколоночную композицию: основной экран занимает всю ширину, презентация и коммуникация расположены рядом. Единая высота заголовочной зоны сохранена, поэтому начало макетов остаётся выровненным.

Проверено на viewport 390 px: большой пустой светлый блок исчез, общая карточка стала существенно ниже, переход к секции «05 / Передача результата» приблизился. `npm run lint` и `git diff --check` проходят. Снимок: `artifacts/design-mobile-flow-compact-equal.png`. Публикация и push не выполнялись.

## Рабочий скелет страницы SEO-продвижения — 14 сентября 2026

Создана новая внутренняя страница `/services/seo` в утверждённом тёмном editorial-стиле ABB.IO. Маршрут подключён через существующий реестр услуг и динамическую страницу `[slug]`; добавлены отдельные title и description. Согласованные тексты сохранены без обещаний позиций, сроков, трафика или количества заявок.

Страница включает полноэкранный desktop hero, восемь смысловых разделов, нейтральную перелинковку на подтверждённые SEO-контексты OSS и «Волхонки» и финальную CTA «Обсудить SEO». Финальные AI-изображения не создавались: поисковый спрос, архитектура страниц, техническая проверка, контент, путь до CRM, цикл развития и собственный канал показаны смысловыми HTML/CSS-схемами. Каждый блок отделён тонкой линией и использует общую трёхколоночную сетку заголовка.

Проверены desktop 1280×720 и mobile 390×844: root horizontal overflow отсутствует, H1 и заголовки переносятся без обрезания, мобильное меню раскрывается и закрывается по Escape, CTA открывают контактный диалог, focus-visible отображается, внутренние ссылки ведут на `/services`, `/cases/oss` и `/cases/volhonka`. Console errors/warnings отсутствуют; прямое открытие `/services/seo` возвращает 200. `npm run lint` и production `npm run build` проходят, маршрут входит в SSG. Снимки: `artifacts/seo-desktop-full.png`, `artifacts/seo-mobile-full.png`. Публикация, commit и push не выполнялись.

## Смысловая основа и единый ритм страницы «Сайты» — 13 сентября 2026

Владелец определил текущую `/services/design` как пример собранной внутренней landing-страницы и поручил привести `/services/websites` к сопоставимой структурной дисциплине без копирования визуалов. На `/services/websites` секции 01–08 теперь используют один компонент заголовка: метка слева, H2 на общей оси и короткое пояснение справа; на mobile порядок становится линейным. Для этих секций установлены единые вертикальные интервалы 122 px на desktop и 76 px на mobile, после каждого смыслового блока добавлен тонкий разделитель.

Тексты уточнены по обратной связи владельца. Форматы сайта больше не привязаны только к формулировке о продажах бизнеса; интеграция охватывает форму, почту, звонки и чат с передачей согласованного контекста в CRM; блок после запуска сокращён до готовых страниц, доступов и основы для развития; результат сформулирован как рабочий цифровой актив компании. Финальный CTA теперь предлагает обсудить сайт как актив, а не разовую публикацию. Hero, подтверждённые платформы и кейс ОборонСпецСплава сохранены по смыслу.

При QA обнаружено, что общий `ContactDialog` открывался через CSS-якорь, но не закрывался клавишей Escape и не возвращал фокус. Диалог переведён на штатный `showModal()`: фокус ставится в первое поле, Escape закрывает окно, а фокус возвращается к вызвавшему CTA. Содержание формы и отсутствие backend-отправки не менялись.

Проверены 1440×900, 1280×720, 1024×768, 390×844, 360×800 и 320×700: оси заголовков и интервалы совпадают, разделители присутствуют, root horizontal overflow равен нулю, изображение кейса загружается. На 390×844 проверены меню, Escape, открытие CTA, фокус первого поля, закрытие и возврат фокуса. Console errors/warnings на чистой вкладке отсутствуют. `npm run lint`, production `npm run build` и `git diff --check` проходят. Главная, `/services/design`, `/services/marketing`, публикация и внешние сервисы не изменялись. Следующий этап `/services/websites` — отдельная визуальная доработка внутренних механик после просмотра владельцем.

## Hero-визуал `/services/websites` — 13 сентября 2026

Схематичный правый маршрут hero заменён на сгенерированный визуал `public/services/websites-hero-sales-workflow-v2.png`. Это не реальный интерфейс клиента: он образно показывает сайт в центре пути «предложение → выбор → новый запрос → рабочая среда → менеджер». После обратной связи владельца в финальной версии удалены человек, стол и бытовые детали; этап работы менеджера показан абстрактной workflow-схемой. В `WebsitesServicePage.tsx` подключён новый `next/image` с пустым alt, потому что изображение декоративно и смысл уже передан H1 и основным текстом. В CSS visual вынесен из grid в самостоятельный полноразмерный слой hero: он начинается от правого края, занимает всю высоту первого экрана и мягко растворяется под текстом слева без обрезания исходной композиции. На mobile становится самостоятельным блоком после CTA.

Контроль: 1280px и 390×844, `scrollWidth === clientWidth` на mobile, asset загружается, console ошибок/предупреждений нет. После визуальной коррекции image закреплён по правому краю hero и масштабирован до 82%. Для него добавлен `quality={100}`, а `next.config.ts` разрешает качество 100; фактический URL локального оптимизатора подтверждает `q=100`. `npm run lint` и `npm run build` проходят. Публикация и push не выполнялись.

Следующее уточнение владельца: у visual были видны верхняя и нижняя границы исходного растра. В `.heroVisual::before` добавлен отдельный вертикальный градиент в цвет фона hero: он полностью уходит в фон в крайних 8% сверху и снизу, мягко растворяется к центральной части и не меняет текст или CTA. Browser-preview подтвердил отсутствие резких верхней и нижней границ; `npm run lint` проходит.

В `.salesSteps` добавлен hover feedback: при наведении карточка сдвигается на 10 px вправо, получает мягкий локальный violet glow, а правый круглый маркер слегка увеличивается и светится. Это только desktop-pointer эффект (`hover:hover`) и только при `prefers-reduced-motion: no-preference`; для reduced motion все относящиеся переходы отключены. `npm run lint` и `npm run build` проходят.

## Иллюстрации форматов сайтов — 13 сентября 2026

В блоке «02 / Форматы сайтов» на `/services/websites` прежние абстрактные CSS-макеты заменены тремя сгенерированными иллюстративными preview: лендинг фотостудии, корпоративный сайт нефтеперерабатывающей компании и каталог смартфонов. Это не клиентские проекты и не коммерческие предложения конкретных брендов. Assets подключены как декоративные `next/image` с пустым alt: содержание карточек уже передано заголовками и текстом. Desktop-preview уменьшен до 165 px, поэтому двухстрочный заголовок «Корпоративный сайт» не перекрывается. Все три файла загружаются, при ширине 1280 px root horizontal overflow отсутствует; `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

После следующего уточнения владельца карточки форматов больше не используют фиксированную высоту preview сверху. Они собраны grid-раскладкой «номер → изображение → текст»: иллюстрация занимает всё свободное пространство карточки, а не оставляет пустое поле перед короткими заголовками «Лендинг» и «Каталог». В desktop-контроле высоты preview составили 274 px / 157 px / 274 px соответственно, что отражает разный объём текста, не обрезая его. Root horizontal overflow отсутствует; `npm run lint` и production `npm run build` проходят.

## Иконки в блоке технологий — 13 сентября 2026

По просьбе владельца из трёх карточек технологий `/services/websites` удалены номера. В их верхней центральной зоне добавлены локальные SVG-знаки React, 1С-Битрикс и WordPress. Порядок карточек установлен как «React / Next.js → 1С-Битрикс → WordPress»; вместе с карточками перенесены их тексты. SVG декоративны (`aria-hidden`), поскольку название каждой платформы уже есть в H3. Проверка в браузере подтвердила расположение и отсутствие horizontal overflow; `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Фоновые иллюстрации CRM-маршрута — 13 сентября 2026

По просьбе владельца в трёх карточках блока «05 / Интеграция сайта» добавлены сгенерированные фоновые UI-сцены. Первая показывает слияние формы, почты, звонка и чата в контакт; вторая — источник и детали запроса как связанный набор данных; третья — рабочую CRM с карточкой контакта и следующими этапами. Каждая сцена подключена локально через `next/image` с пустым alt и накрыта тёмным градиентом: смысл остаётся в доступном тексте, а картинка не мешает его контрасту. Все три assets загружаются (384×216 в desktop-preview), root horizontal overflow отсутствует; `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Иллюстрации этапов создания сайта — 13 сентября 2026

В блоке «03 / Создание сайта» шесть абстрактных CSS-превью заменены единым набором сгенерированных горизонтальных иллюстраций: разбор бизнеса и обращений, архитектура страниц и материалов, сценарий с прототипом, дизайн-макет, адаптивные desktop/mobile-экраны и тестирование с запуском. Все изображения вымышленные, декоративные и не представляют клиентские проекты или конкретные продукты. Они подключены через `next/image` с пустым alt; подробный смысл каждого этапа остаётся в соседнем H3 и описании. Превью имеют пропорцию 16:9, номер этапа сохранён поверх изображения, последовательный маршрут и соединители не менялись. Browser QA: все шесть изображений загружены (200×112 в desktop-preview), root horizontal overflow отсутствует. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Передача сайта и подготовка к SEO — 13 сентября 2026

По замечанию владельца полностью переработан только визуал блока «06 / После запуска» на `/services/websites`. Светлая панель с неиспользуемой правой половиной заменена цельной тёмной handoff-композицией: декоративный visual готового desktop/mobile-сайта, доступов и материалов расположен рядом с компактным перечнем передаваемого результата; блок SEO стал нижней частью того же объекта. Согласованные заголовки, три результата и формулировка о подготовке сайта к SEO сохранены. Новая иллюстрация `public/services/websites-post-launch-handoff-v1.png` сгенерирована встроенным imagegen: это вымышленная декоративная UI-сцена без людей, брендов, показателей или клиентских интерфейсов, поэтому она имеет пустой alt.

Проверены 1440×900, 1024×768, 390×844 и 320×700: root и локальный horizontal overflow отсутствуют, изображение загружается после попадания блока в viewport, SEO-ссылка корректно переносится и headings остаются последовательными. Hover-microinteraction visual включается только для точного указателя и `prefers-reduced-motion: no-preference`; при reduced motion изменений нет. Console errors/warnings отсутствуют. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Переход в услугу «Дизайн» — 13 сентября 2026

По просьбе владельца блок «07 / Дизайн и выбор» на `/services/websites` перестроен из внутренней CSS-схемы в самостоятельный переход к `/services/design`. Согласованный заголовок и объяснение о предложении и выборе сохранены; под ними размещён единый кликабельный portal: реальный иллюстративный visual `public/services/design-hero-volterra-wide-v7.png`, уже используемый на странице «Дизайн», и ясный CTA «Нужен самостоятельный дизайн-этап? Перейти к дизайну». Так блок не выдаёт абстрактную заглушку за результат работы и сразу показывает характер целевого раздела.

Portal оформлен как самодостаточный паттерн для последующего использования на других услугах: крупный visual, короткая причина перейти и единая ссылка по всей площади. На desktop его hover ограничен устройствами с точным указателем и отключён при `prefers-reduced-motion`; `focus-visible` имеет явный контур. На mobile композиция складывается в visual и CTA без пустой боковой колонки. Проверены 1440×900, 390×844 и 320×700: root horizontal overflow отсутствует, visual загружается, переход ведёт на `/services/design`, console errors/warnings на чистой вкладке отсутствуют. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Иконки в результате проекта — 13 сентября 2026

По просьбе владельца свободные правые области четырёх карточек блока «08 / Результат проекта» на `/services/websites` заполнены четырьмя отдельными предметными иконками: архитектура страниц, точки обращения, интеграция и материалы для развития. Для них созданы локальные transparent PNG: `websites-delivery-structure-icon-v1.png`, `websites-delivery-contact-icon-v1.png`, `websites-delivery-integration-icon-v1.png`, `websites-delivery-materials-icon-v1.png`. Это вымышленные декоративные 3D-объекты без людей, текстов, логотипов, брендов или клиентских интерфейсов; alpha-канал сохранён, поэтому у изображений нет фоновой плашки.

Иконки подключены через `next/image` с пустым alt внутри `aria-hidden`-контейнеров, поскольку смысл карточек передаётся заголовками и описаниями. На desktop они занимают прежнее свободное пространство справа; на mobile уменьшаются вместе с внутренним отступом карточки. Hover-увеличение работает только для точного указателя и при отсутствии reduced motion. Проверены 1440×900, 390×844 и 320×700: все четыре файла загружаются, текст не пересекается с иконками, horizontal overflow отсутствует, console errors/warnings нет. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Брендовые знаки платформ — 13 сентября 2026

Владелец уточнил: в карточках «React / Next.js», «1С-Битрикс» и «WordPress» нужны не сгенерированные и не условно нарисованные символы, а узнаваемые знаки платформ. Поэтому прежний `PlatformIcon` удалён. В `WebsitesServicePage.tsx` добавлена локальная карта `platformLogos`, которая выводит изображения через `next/image`:

- `public/services/websites-platform-react.svg` — React из [Simple Icons](https://simpleicons.org/), скачан локально;
- `public/services/websites-platform-1c-bitrix.svg` — SVG favicon с официального [сайта 1С-Битрикс](https://www.1c-bitrix.ru/favicon.svg);
- `public/services/websites-platform-wordpress.png` — официальный белый W Mark со страницы [графики WordPress](https://wordpress.org/about/logos/).

Файлы не загружаются во время просмотра страницы со сторонних серверов. Все три изображения декоративны: `alt=""`, окружающий контейнер уже `aria-hidden`, а доступное имя платформы дано в H3. У официального W Mark есть прозрачный внешний отступ, поэтому только он увеличен в CSS до 86 px внутри общего 64 px контейнера — фактический знак по оптическому размеру совпадает с React и 1С-Битрикс.

Проверка: локально все три знака загрузились, console errors/warnings = 0; при 390×844 и 320×700 `scrollWidth === clientWidth`. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Более приземлённый visual «После запуска» — 13 сентября 2026

По последнему замечанию владельца прежняя футуристическая иллюстрация `public/services/websites-post-launch-handoff-v1.png` больше не используется в блоке «06 / После запуска» на `/services/websites`: она выглядела как условный 3D-рендер. V1 сохранён в проекте, а компонент теперь использует `public/services/websites-post-launch-handoff-v2.png`. Новый visual сгенерирован встроенным imagegen как приземлённая редакционная фотография рабочего места: desktop- и mobile-версия одного нейтрального сайта, распечатанные схемы страниц, папка передачи и материалы для дальнейшей работы. В сцене нет брендов, клиентских данных, показателей, читаемого текста или обещаний результата; частично видна только рука с ручкой, без лица или человека в кадре.

Проверены загрузка новой иллюстрации, desktop 1440×900 и mobile 390×844: на mobile `scrollWidth === clientWidth`, изображение корректно загружено и console errors/warnings отсутствуют. Публикация и push не выполнялись.

## Усиленный переход в услугу «Дизайн» — 13 сентября 2026

По новой обратной связи владельца переработан только внутренний portal блока «07 / Дизайн и выбор» на `/services/websites`. Его прежняя правая колонка была визуально пустой: короткий текст и рядовая ссылка не соответствовали масштабу visual слева. Теперь внутри неё есть второй, более крупный фрагмент существующей AI-концепции дизайн-доски `public/services/design-hero-volterra-perspective-v9.png`, а переход в `/services/design` оформлен как контрастная широкая CTA-кнопка. Левый visual `design-hero-volterra-wide-v7.png`, заголовок, объяснение, URL и доступное имя ссылки сохранены.

Весь portal остаётся одной семантической ссылкой, без вложенной кнопки: Tab попадает на неё один раз, `focus-visible` сохраняет явный контур, а изображения имеют пустой alt внутри декоративных контейнеров. Hover добавляет только мотивированные feedback-состояния: общий подъём, очень небольшое увеличение двух visual и отклик CTA; они включены лишь для точного указателя и при `prefers-reduced-motion: no-preference`. Проверены desktop 1440×900, mobile 390×844, загрузка двух изображений и root overflow (`scrollWidth === clientWidth` на mobile). `npm run lint` и production `npm run build` проходят. В dev-console есть единственное ранее существовавшее предупреждение Next.js о React-логотипе как LCP на этой длинной странице, оно не относится к portal и не появилось из этой правки. Публикация и push не выполнялись.

## Единый блок кейсов со страницей «Дизайн» — 13 сентября 2026

По прямой просьбе владельца одиночная split-композиция кейса ОборонСпецСплава на `/services/websites` заменена существующим shared-компонентом `CasePortfolio`, который уже использует `/services/design`. Новый отдельный блок не создавался, CSS `CasePortfolio.module.css` не менялся. На «Сайтах» компонент получает те же три подтверждённых кейса, но список начинается с `oss`, поэтому крупной первой карточкой остаётся ОборонСпецСплав, как релевантный каталожный пример страницы. Остальные карточки ведут на Мотошколу Владимира Богова и Металлобазу Волхонка.

Проверены 1440×900 и 390×844: каркас совпадает со страницей «Дизайн», все три карточки имеют доступные имена и ссылки на `/cases/oss`, `/cases/bogov`, `/cases/volhonka`; на mobile `scrollWidth === clientWidth`. `npm run lint` и production `npm run build` проходят. Console errors отсутствуют; предупреждение Next.js про LCP React-логотипа было до этой правки и к блоку кейсов не относится. Публикация и push не выполнялись.

## Удаление финальной CTA-панели «Сайты» — 13 сентября 2026

По прямой просьбе владельца с `/services/websites` удалена только финальная CTA-панель с заголовком «Сайт должен стать активом компании, а не разовой публикацией». После общего блока кейсов теперь сразу следует существующий footer. Header CTA и форма в footer не менялись; все остальные секции страницы сохранены. Browser-проверка на 1440×900 подтверждает отсутствие удалённой разметки, наличие footer и отсутствие root horizontal overflow. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Контактный диалог без внутренней прокрутки — 13 сентября 2026

По прямому требованию владельца общий `ContactDialog` больше не использует внутреннюю прокрутку при первоначальном открытии. На desktop форма собрана в две колонки: объяснение слева, поля и submit справа. На ширинах до 700 px она становится компактной одноколоночной: сокращены отступы и интервалы, сохранены 44 px для текстовых полей и 48 px для кнопки, а textarea имеет достаточную высоту для исходного placeholder без scrollbar. На 320×700 даже две ошибки валидации помещаются полностью, не обрезаются и не требуют прокрутки диалога. `Escape`, возврат фокуса на CTA и поля с ошибками сохранены. Проверены 1440×900, 1280×720, 1024×768, 390×844, 360×800 и 320×700: dialog и textarea без initial scrollbar, root horizontal overflow отсутствует. `npm run lint` и production `npm run build` проходят. В dev-console осталось одно ранее существовавшее предупреждение Next.js о React-логотипе как LCP; к диалогу оно не относится. Публикация и push не выполнялись.

## Упрощённый CTA-переход в «Дизайн» — 13 сентября 2026

По следующему замечанию владельца из правой части portal блока «07 / Дизайн и выбор» на `/services/websites` удалён второй preview `design-hero-volterra-perspective-v9.png`: он был лишним и отвлекал от перехода. В portal сохранён главный visual слева, а правая колонка теперь содержит только короткий вопрос и крупную CTA «Перейти к дизайну». Сам asset не удалён, потому что продолжает использоваться на `/services/design`.

Для CTA добавлен мотивированный hover effect: карточка слегка поднимается, кнопка получает локальное сиреневое свечение, а стрелка смещается по направлению перехода. Эффекты задействуют только `transform`, `background` и `box-shadow`, включены исключительно при точном указателе и `prefers-reduced-motion: no-preference`; явный focus-visible сохранён. Проверены desktop 1440×900 и mobile 390×844, 360×800 и 320×700: внутренний preview отсутствует, CTA не выходит за границы и root horizontal overflow отсутствует. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Смысловая и композиционная основа страницы «Маркетинг» — 13 сентября 2026

По обратной связи владельца `/services/marketing` приведена к той же заголовочной сетке и вертикальному ритму, которые уже закреплены на внутренних страницах «Дизайн» и «Сайты». Секции 01–08 используют единый локальный `SectionHeader`: индекс находится слева, H2 — на общей центральной оси, краткое пояснение — в правой колонке. У секций одинаковые desktop/mobile-отступы и спокойные нижние разделители; светлые секции получили отдельный контраст текста. На ширинах до 1080 px пояснение переходит под заголовок в его колонке, на mobile блок складывается в последовательность индекс → заголовок → пояснение.

В пользовательском тексте убрана привязка услуги к Битрикс24: hero, карта источников, SEO-маршрут, рабочий контур, цикл улучшений и результат теперь говорят о CRM как о классе решений. Логика страницы уточнена без выдуманных показателей: источники сходятся в одной системе, путь клиента идёт от первого касания к обращению, блок показателей сравнивает результат каналов вместо отдельных кликов, отчёт поддерживает управленческое решение, а финальный состав описывает то, что остаётся у команды после запуска. Повторы слов «заявка» и «обращение» сокращены там, где они не добавляли нового смысла.

Проверены 1440×900, 1280×720, 1024×768, 390×844, 360×800 и 320×700. На 1440 и 1280 все восемь H2 и все правые пояснения имеют общие X-координаты; каждая секция использует 122 px вертикального поля и разделитель 1 px. На mobile поля составляют 20/16 px, вертикальное поле — 76 px, root horizontal overflow отсутствует. Изображения загружаются, мобильное меню закрывается по Escape, контактный диалог открывается с фокусом на первом поле и возвращает фокус на CTA после закрытия. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Mobile hero «Сайты» по ритму «Дизайна» — 14 сентября 2026

По прямому уточнению владельца mobile hero `/services/websites` перестроен по ритму утверждённой страницы «Дизайн»: после H1 сначала расположен visual, поясняющий текст читается в его нижней части, а CTA «Обсудить новый сайт» находится ниже visual. Desktop-подача сохранена: изображение остаётся полноэкранным фоновым слоем справа и не перекрывает коммерческий текст.

Проверены 1440×900, 390×844 и 320×700: root horizontal overflow отсутствует; на 320 CTA полностью находится в пределах hero, на 390 диалог открывается без внутренней прокрутки, `Escape` закрывает его и возвращает фокус на CTA. Console errors/warnings страницы отсутствуют. Круглый значок `N` в левом нижнем углу локального preview — кнопка Next.js Dev Tools, доступная только в dev-режиме и не входящая в production-страницу. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Предзагрузка preview форматов на mobile — 14 сентября 2026

Исправлена пустая иллюстрация в mobile-ленте «02 / Форматы сайтов». Причина была в lazy loading `next/image`: карточка могла попасть в горизонтальный viewport до завершения запроса картинки. Три декоративных preview форматов (лендинг, корпоративный сайт, каталог) теперь используют `loading="eager"`. У изображений сохранён пустой `alt`, поскольку доступный смысл уже передаётся заголовками и описаниями карточек.

В свежей mobile-сессии 390×844 до ручной прокрутки все три изображения получили `complete: true` и реальные естественные размеры; отдельно проверен переход к «Корпоративному сайту» — его иллюстрация отображается. Root horizontal overflow и console errors отсутствуют. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Предзагрузка этапов создания сайта на mobile — 14 сентября 2026

Исправлена пустая иллюстрация «Сценарии и прототип» в блоке «03 / Создание сайта». У шести preview этапов сохранялся lazy loading, поэтому любой из них мог показать рамку раньше картинки при первом попадании секции в viewport. Вся серия из шести декоративных иллюстраций теперь использует `loading="eager"`; пустой `alt` сохранён, так как название и смысл каждого этапа передаются соседними H3 и текстом.

В новой mobile-сессии 390×844 ещё до прокрутки все шесть изображений получили `complete: true` и естественные размеры 165×92 px; после прокрутки визуально подтверждено изображение в третьей карточке. Root horizontal overflow и console errors отсутствуют. `npm run lint` и production `npm run build` проходят. Публикация и push не выполнялись.

## Рабочая страница Яндекс Директа — 14 сентября 2026

Добавлена новая внутренняя страница `/services/yandex-direct` с отдельной композицией в визуальном языке ABB.IO. Полноэкранный hero объясняет путь от спроса до отчёта; восемь последовательных разделов раскрывают задачу и предложение, места показа, связь объявления с посадочной страницей, структуру кампаний, передачу целевых действий в CRM, состав отчёта, регулярное управление и ожидаемый результат. В блоке связанных направлений используются существующие страницы «Сайты», «Дизайн» и «SEO-продвижение»: подтверждённого кейса по Яндекс Директу в проекте нет, поэтому он не придумывался.

Интерфейсные иллюстрации собраны из HTML/CSS и не содержат фиктивных клиентских показателей, бюджетов, цен или гарантий. В отчёте использованы прочерки и явная оговорка о данных CRM. Для страницы добавлены metadata, запись в реестр услуг и статическая маршрутизация через `src/app/services/[slug]/page.tsx`; визуал и карточки `/services`, главная и другие внутренние страницы не менялись.

Проверены desktop 1280×720 и mobile 390×844: root horizontal overflow отсутствует, H1 и все секции читаются, мобильное меню открывается и закрывается по Escape, CTA открывает контактный диалог, ссылка «Как выглядит отчёт» приводит к целевому разделу, внутренние ссылки работают, focus-visible имеет явный контур, console errors/warnings отсутствуют. `npm run lint` и production `npm run build` проходят; `/services/yandex-direct` входит в статическую генерацию. Проверочные снимки сохранены в `artifacts/yandex-direct-desktop-full.png` и `artifacts/yandex-direct-mobile-full.png`. Публикация, commit и push не выполнялись.

## Карточка SEO-продвижения на `/services` — 14 сентября 2026

В существующий список направлений на корневой странице услуг добавлена четвёртая карточка SEO-продвижения. Три прежние услуги, их тексты и порядок сохранены; верхняя компактная карта по-прежнему показывает исходные три направления. SEO-карточка следует после «Маркетинга», ведёт на `/services/seo`, использует текущую структуру карточки, локальную CSS-схему поискового спроса и отдельную ссылку «Подробнее об SEO» с явным `focus-visible`.

На desktop 1280×720 второй ряд состоит из двух равных по ширине и высоте карточек «Маркетинг» и SEO; после уточнения переноса их общая высота составляет 453 px. На mobile 390×844 карточки складываются в одну колонку шириной 335 px, root `scrollWidth === clientWidth` (375 px). Переход открывает страницу SEO с корректным title, а чистая браузерная сессия не содержит console errors/warnings. `npm run lint` и production `npm run build` проходят. Публикация, commit и push не выполнялись.

## Карточка Яндекс Директа и порядок услуг на `/services` — 14 сентября 2026

В основной список направлений добавлена пятая карточка «Контекстная реклама» с согласованными заголовком, описанием и CTA «Подробнее о рекламе» на `/services/yandex-direct`. Карточка использует тот же компонент и состояния ссылки, а собственный CSS-акцент показывает маршрут «Объявление → Обращение → Отчёт». Порядок основного списка и верхней компактной карты приведён к решению владельца: Сайты → Маркетинг → Дизайн → SEO-продвижение → Яндекс Директ; верхняя карта, как и раньше, содержит только первые три самостоятельных направления. Тексты существующих услуг не менялись.

На desktop 1280×720 первые четыре карточки образуют два парных ряда 5/7 и 7/5 колонок, Директ занимает отдельный широкий ряд на 12 колонок. Высоты внутри каждого парного ряда совпадают: 405 и 453 px; карточка Директа — 410 px. На mobile 390×844 все пять карточек идут одной колонкой шириной 335 px, root `scrollWidth === clientWidth` (375 px). CTA Директа получает явный `focus-visible`, прямой переход открывает страницу с корректным title, console errors/warnings отсутствуют. Публикация, commit и push не выполнялись.
